<h4 class="text-capitalize">master jenis buku</h4>

<div class="row mt-4">
    <div class="col-12">
        <div class="row">
            <div class="col-lg-6 text-end">
                <a href="/master/jenis-buku/tambah" class="btn btn-sm btn-primary">
                    <i class="fa fa-fw fa-plus-circle"></i> jenis baru
                </a>
            </div>
        </div>
    </div>
    <div class="col-12">
        <div class="row">
            <div class="col-lg-6">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>kode</th>
                            <th>nama</th>
                            <th>option</th>
                            <th>hapus</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for( let i=0; i < jenis_buku.length; i++) { %>
                            <tr>
                                <td><%= jenis_buku[i].kode %></td>
                                <td><%= jenis_buku[i].nama %></td>
                                <td>
                                    <a href="/master/jenis-buku/detail/<%= jenis_buku[i].id %>" class="btn btn-sm btn-outline-info">
                                        <i class="fa fa-fw fa-solid fa-eye"></i>
                                    </a>
                                    <a href="/master/jenis-buku/edit/<%= jenis_buku[i].id %>" class="btn btn-sm btn-outline-warning">
                                        <i class="fa fa-fw fa-solid fa-edit"></i>
                                    </a>
                                </td>
                                <td>
                                    <form action="/master/jenis-buku/delete/<%= jenis_buku[i].id %>" method="post">
                                        <button type="submit" class="btn btn-sm btn-outline-danger"
                                        onclick="return confirm('apakah anda yakin ingin hapus <%= jenis_buku[i].nama %>?')">
                                            <i class="fa fa-fw fa-solid fa-trash"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
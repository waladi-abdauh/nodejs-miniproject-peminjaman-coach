<h4 class="text-capitalize">detail genre</h4>

<div class="row">
    <div class="col-lg-6">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title"><%= genre[0].nama %></h5>
                <p class="card-text">
                    Kode: <%= genre[0].kode %> <br>
                    waktu pembuatan: <%= moment(genre[0].created_at).format('DD MMMM YYYY HH:mm') %> <br>
                    dibuat oleh: <%= genre[0].created_nama %>
                </p>
            </div>
        </div>
    </div>
</div>